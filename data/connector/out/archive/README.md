# crosswalk 발행 아카이브 — 이름이 내용을 가리키게 (2026-08-17 신설)

**왜 있나**: `crosswalk_v0.2.json`이라는 **한 이름이 3일 사이 3개 내용**을 가리켰다.
파일명도 `v0.2`도 안 바뀌었고 제자리 덮어쓰기라 직전판 실물이 사라졌다.

★**가설이 아니라 이미 비용이 났다** — 08-17에 leomusic-trot이 「직전판과 동일한가」를
대조하려 했으나 그 직전판이 같은 이름으로 덮여 **직접 대조를 못 하고 연쇄 대조로 우회**했고,
그만큼 증거 등급이 내려갔다. 요청자(나)와 반증자 **양쪽 다 그 파일을 못 지목**했다.

**같은 뿌리의 한 층 위**: 08-16~17에 내가 고친 것은 「값에서 한정자가 떨어진다」였다.
이건 그 위다 — ★**식별자에서 내용이 떨어져 있다.** `v0.2`는 자기가 무엇인지 말하지 못한다.

**규약**
- 발행마다 `crosswalk_v{iv}.{sha256[:8]}.json`으로 **불변 사본**을 남긴다(`corpus_connector.py out publish`).
- 같은 내용 재발행은 덮어쓰지 않는다.
- 기존 경로 `../crosswalk_v0.2.json`·`../crosswalk_latest.json`은 **그대로 둔다** — 소비자 무영향(additive).
- 파일명의 8자는 **`_manifest` 제거 후 `sort_keys` 직렬화**의 sha256 앞 8자다. 파일 해시가 아니다.

**소급 복원 3판** (전건 자가검증 통과 = 파일명 해시와 내용 해시 일치)
| 판 | sha256[:8] | 크기 | 출처 |
|---|---|---|---|
| 08-15 최초 발행 | `d1c4e9ad` | 331,154B | git `59c3dff` 실물 |
| 08-17 1차(registers.attestation) | `bb3ab29a` | 331,455B | 재발행 직전 내 보관본 |
| 08-17 2차(attested_count_scope) | `b172510e` | 416,195B | 현행 |

★**「소실」도 조회 범위 문제였다** — trot도 나도 직전판이 없어졌다고 봤는데, git이 08-15판 실물을
갖고 있었다. 「없다」가 나오면 대상 전에 조회 범위를 의심한다(오늘 5회차).
