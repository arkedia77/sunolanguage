#!/usr/bin/env python3
"""지시축 대장 v1.3 — leomusic3 uptake 실측 편입.

★왜: v1.2 §6에 「브라켓이 **먹힌다**고는 안 했다 — 잰 것은 **안 불린다**(부재)뿐이고
   uptake는 미측정」이라 적었다. leomusic3가 그 층을 **강제정렬**로 재서 보내 왔다(08-27).

★근거 등급 M4 신설 — 「타 프로젝트 실측(내가 재현 못 함)」.
   M3는 **외부 실물을 내가 직접 잰 것**이다(X1 유튜브 자막). leomusic3 값은
   **그쪽 자·그쪽 오디오·그쪽 측정**이라 내가 다시 못 돌린다. 같은 칸에 두면
   나중에 내가 잰 것처럼 인용된다. ⇒ 칸을 나눈다.

생성 0 · 크레딧 0 · 새 수집 0.
"""
import json
from collections import Counter

SRC='data/metatag_external/directive_register_v1_2.json'
OUT='data/metatag_external/directive_register_v1_3.json'

L3 = {
 '출처':'leomusic3 → sunolanguage, 2026-08-27T18:53:19 '
        '(agent-comm:projects/sunolanguage/messages/processed/sunolanguage_leomusic3_20260827_185319_'
        '가이드v1접수_uptake실측2건_성별층단서_§2해당없음.json)',
 '표본':'E3009M — en_celtic_2step **10곡** · BPM 136~140 · 강제정렬 v3',
 '★uptake':'설계 브라켓 **30개 전건**이 렌더 가사에 원문 그대로 들어갔고, '
            '**28/30에서 실제 무가창 구간이 계측**(중간 브라켓 11건 중 9건 · 중앙 **14.1초**). '
            '미검출 2건은 **gap 0.0초** — 렌더가 직전 음절 끝과 직후 시작을 이어 불렀다.',
 '★bar 수':'`[Tin Whistle Intro 8bar]`의 bar 수를 BPM으로 환산한 이상치 대비 **실제 무가창 길이 중앙 2.86배**'
            '(범위 0.23~5.34배). ★**짧게 지시할수록 더 벗어난다** — 4bar 지시(7.0s) 3.06~5.34배 / '
            '8bar 지시(14.0s) 1.55~2.66배. 하한이 있는 것처럼 움직이고, 지시보다 짧았던 예외는 1곡.',
 '부수':'도입 무가창 중앙 29.8초 · Outro 중앙 23.0초 — 192초 트랙에서 무가창이 약 1/4.',
 '★그쪽 본인의 정리':'「길게 쓰는 것과 길이를 지시하는 것은 다른 축이다 — 서술을 늘리는 건 안전하고, '
                      '숫자로 길이를 지시하는 건 안 먹힌다.」',
 '⛔★내가 붙이는 한계 3가지':[
   '★**음성 대조가 없다** — 「브라켓이 구간을 만든다」와 「그 위치에 원래 구간이 생긴다」가 **안 갈렸다**. '
   '브라켓 없는 같은 위치의 gap을 안 쟀다. ⇒ 내가 `[Intro]`에서 겪은 것과 **같은 형태**다'
   '(입력 있음 97.9% vs 입력 없음 **97.7%** — 차이 0이었다). 다만 **중간 브라켓 9건·중앙 14.1초**는 '
   '기본 거동으로 보기엔 길다 ⇒ 「무근거」가 아니라 「미분리」다.',
   '★**긴 브라켓 팔이 아니다** — 내가 아는 문면은 `[Tin Whistle Intro 8bar]`(4어) 하나뿐이고 '
   '30개의 길이 분포를 못 받았다. ⇒ **「긴 브라켓은 noise로 압축된다」(D013)에 대한 답이 아니다.**',
   '★**M4다 — 내가 재현할 수 없다.** 그쪽 오디오·그쪽 강제정렬·그쪽 판정이다. '
   '인용 시 반드시 출처를 붙이고, **내가 잰 것처럼 적지 않는다.**'],
 '★독립성':'우리 값(텍스트 층·입력↔출력 짝 411)과 이 값(오디오 층·강제정렬)은 '
            '**다른 자·다른 층·다른 렌더**다. 같은 방향이면 그건 재계수가 아니라 **독립 확증**이다.',
}

A={}
def s(i,v,ax,gr,dr,ct,why,extra=None):
    A[i]=dict(판정=v,**{'M-축':ax,'M-급':gr,'M-방향':dr,'M-대조군':ct},사유=why,추가=extra or {})

s('D052','already_measured','일치','M1 전수 + ★M4 타프로젝트 실측','존재+부재','⒜★있음 / ⒝없음(음성대조 미보고)',
  '★**이 대장에서 근거가 가장 두꺼운 자리가 됐다.** 주장 두 갈래가 **서로 다른 두 자**로 각각 걸린다 — '
  '⒜「브라켓 없으면 지시를 노래한다」=우리 텍스트 층(대조 395/536=73.69% ↔ 처치 0/4,166) '
  '⒝「[ ] 안은 **큐로 읽고** 안 부른다」의 **「큐로 읽고」**=leomusic3 오디오 층(28/30 무가창 구간 계측). '
  '⇒ 「안 불린다」(부재)에서 **「구간을 실제로 만든다」(존재)**까지 갔다. 부재는 약한 증거지만 존재는 그렇지 않다.',
  {'★남은 것':'⒝에 음성 대조가 없다. 그것만 붙으면 이 자리는 대장에서 유일하게 양층·양방향·대조군 완비가 된다.'})

s('D004','already_measured','일치','M1 전수 + ★M4 타프로젝트 실측','부재(ⓐ) + 존재(ⓑ)','ⓐ★있음 / ⓑ없음',
  '★**v1.2에서 「ⓑ는 여전히 미측정」이라 적어 둔 자리가 열렸다.** ⓐ「안 부른다」는 우리 전수(0/4,166), '
  'ⓑ「**읽고 반응한다**」는 leomusic3 강제정렬(30개 전건 렌더 반영·28/30 무가창 계측). '
  '⇒ 주장 두 갈래가 다 걸렸다. ⚠단 ⓑ는 **음성 대조가 없어** 「브라켓이 만든 구간」과 「원래 생길 구간」이 안 갈렸다.',
  {'★v1.2에서 뭐라 적었나':'「ⓑ「읽고 반응한다」는 여전히 미측정」 — 그 문장을 이 값이 지운다.'})

s('D015','partially_measured','부분','M1 전수(형식) + ★M4(렌더 준수)','부재','해당 없음',
  '★**`measured_offaxis`에서 올라온다.** v1.2에서는 「우리가 잰 것은 **출력 형식 존재**이고 주장은 **입력 준수**라 '
  '축이 다르다」였다. leomusic3가 **렌더 준수**를 쟀다 — `[… 8bar]`의 숫자가 **길이 레버가 아니다**'
  '(중앙 2.86배·짧게 지시할수록 더 이탈). ⇒ 「브라켓 안 숫자 파라미터는 무시된다」에 **렌더 층 근거**가 처음 붙었다. '
  '⚠**M-축이 「일치」가 아니라 「부분」인 이유**: 형태가 다르다 — 주장은 **콜론+숫자**(`[Reverb: 30%]`)이고 '
  '실측은 **인라인 수량**(`8bar`)이다. 같은 것은 **「브라켓 안 숫자 파라미터」라는 상위 범주**까지다.',
  {'★3경로':'⑴외부 주장(hookgenius) ⑵우리 X2 형식 실측 — 출력층 브라켓 9,726 중 콜론+숫자 **0**, 입력층 425 중 **0** '
            '⑶leomusic3 렌더 실측(2.86배). ★셋이 같은 방향이고 **렌더 실측은 ⑶뿐**이다.',
   '★못 하는 말':'「Suno는 숫자를 완전히 무시한다」 — 2.86배는 **무시**가 아니라 **하한이 있는 다른 규칙**처럼 움직인다'
                 '(4bar 지시가 8bar 지시보다 더 크게 벗어났다). 「안 먹힌다」와 「다른 규칙이 있다」는 다르다.'})

s('D051','partially_measured','부분','★M4 타프로젝트 실측','존재','없음',
  '★`needs_generation`에서 올라온다. 「사건 직전에 **간결한 큐 하나**를 두라」는 처방이 leomusic3 문면과 같은 형태이고, '
  '그 형태에서 **28/30 uptake**가 나왔다. ⇒ 처방의 **효과 쪽**에 근거가 붙었다. '
  '⚠단 주장의 핵심은 「구체적 표적 태그가 **넓은 지시 컨테이너보다 낫다**」는 **비교**인데, '
  '비교 팔(규칙블록형)이 없다 ⇒ **우열은 여전히 미측정**.')

s('D013','partially_measured','부분','M1 전수','부재','있음',
  '★근거가 늘긴 했으나 **이 주장에 대한 답은 아니다.** leomusic3 브라켓은 내가 아는 한 **4어급**'
  '(`[Tin Whistle Intro 8bar]`)이고 길이 분포를 못 받았다 ⇒ **「긴 브라켓이 noise로 압축되는가」의 팔이 아니다.** '
  '★다만 주장이 얹어 둔 **「1~3어로 유지하라」**는 처방에는, **4어에서 28/30 uptake**가 **약한 반례**다. '
  '누출 축(우리 전수 6어+ 50건·최장 16어 누출 0)과 합치면 **「짧아야 한다」의 근거가 양쪽에서 얇아진다.**',
  {'여는 조건':'같은 가사·같은 좌표에서 **짧은 브라켓 ↔ 문장형 브라켓**을 돌리고 **uptake를 강제정렬로** 재는 1쌍. '
              'leomusic3가 진입·퇴장형 A/B를 예고했으니 그 설계에 길이 축을 얹으면 한 번에 열린다.'})

s('D024','partially_measured','부분','M1 전수 + ★M4','부재','있음(길이) / 없음(Intro)',
  '두 갈래 중 ⒜「1~2어, 많아야 3어가 최선」에 **4어 28/30 uptake**가 약한 반례로 붙는다. '
  '⒝「`[Intro]`는 덜 신뢰된다」는 **여전히 못 잰다** — 내 음성 대조가 처치를 삼켰다(97.9% vs 97.7%).')

def main():
    d=json.load(open(SRC)); by={c['id']:c for c in d['주장']}
    for i,r in A.items():
        c=by[i]
        c['★v1_2_검증가능성']=c['★검증가능성']
        c['★검증가능성']=r['판정']; c['★검증가능성_근거']=r['사유']
        c['★M']={k:r[k] for k in ('M-축','M-급','M-방향','M-대조군')}
        c['★uptake_근거_leomusic3']=L3
        for k,v in (r['추가'] or {}).items(): c[k]=v
        c['★읽음']='사람(2026-08-28 v1.3)'
    cnt=Counter(c['★검증가능성'] for c in d['주장'])
    d['집계']['★검증가능성']=dict(cnt)
    d['★v1_3_반영']={
      '날짜':'2026-08-28','재현':'scripts/directive_register_uptake_v1_3.py',
      '★생성 0 · 크레딧 0 · 새 수집 0':'leomusic3 회신 1건을 읽고 배치했을 뿐',
      '★무엇이 열렸나':'v1.2 §6의 「브라켓이 **먹힌다**고는 안 했다 — uptake는 미측정」. 그 문장이 지워진다.',
      '★근거 등급 M4 신설':{'정의':'타 프로젝트 실측 — **내가 재현할 수 없다**',
        '왜':'M3(외부 실물)는 **내가 직접 잰 것**이다(X1 유튜브 자막). M4는 남의 자·남의 오디오·남의 판정이다. '
             '같은 칸에 두면 나중에 **내가 잰 것처럼** 인용된다.',
        '규칙':'M4를 인용할 때는 **반드시 출처를 값 옆에 붙인다.**'},
      '★결과':'`measured_offaxis` **1 → 0**(D015 상승) · `needs_generation` **19 → 18**(D051 상승) · '
              '`partially_measured` **5 → 7** · 나머지 불변. 총 61 불변. '
              '★**칸 이동보다 큰 것은 D004·D052의 근거가 두꺼워진 것**이다(부재 → 부재+존재).',
      '★대조군 경고':'leomusic3 값에는 **음성 대조가 없다.** 「브라켓이 구간을 만든다」와 「그 위치에 원래 구간이 생긴다」가 '
                     '안 갈렸다. 내가 `[Intro]`에서 정확히 이것 때문에 값을 못 쓴 적이 있다(97.9% vs 97.7%). '
                     '⇒ **이 대장은 그 한계를 값 옆에 붙여서만 인용한다.**',
      '★남은 것':['D004ⓑ·D052⒝의 음성 대조 — 브라켓 없는 같은 위치의 gap. 이것만 붙으면 대장에서 유일하게 '
                  '양층·양방향·대조군 완비 자리가 된다.',
                  'D013의 길이 축 — 짧은 브라켓 ↔ 문장형 브라켓 uptake 대조.',
                  '위 둘은 leomusic3가 예고한 **진입·퇴장형 A/B**에 얹으면 한 번에 열린다.'],
    }
    json.dump(d,open(OUT,'w'),ensure_ascii=False,indent=1)
    print('WROTE',OUT)
    for k,v in sorted(cnt.items(),key=lambda x:-x[1]): print(f'  {k:<24}{v}')
    print('총',sum(cnt.values()))

if __name__=='__main__': main()
