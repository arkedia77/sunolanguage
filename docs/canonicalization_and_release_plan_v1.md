# 정본화·배포 계획 v1 — 외부 코퍼스 4종의 처분

**작성**: sunolanguage · 2026-08-13
**계기**: 오너 지시 — 「정본화 과정에 필요한 테스트와 청음 분석을 하고, 정본화 배포 계획을 세워서 진행」
**지위**: ★**계획서이지 정본이 아니다.** 이 문서가 정본화하는 것은 **처분 절차**뿐이고, 개별 어휘·문법 판정은 아래 등급대로만 나간다.
**선행 정책**: `corpus_propagation_policy.md`(A/B/C) · `corpus_classification_criteria_v1.md` **v1.1**(§5 판정·§6 passive 승격·★**§11 외부 근거 등급 대장**=본 §2의 편입처)

---

## §0. 왜 계획서가 먼저인가 — 이 코퍼스는 전파 정책의 대상이 아니다

| 정책 | 문면 | 이 코퍼스 |
|---|---|---|
| 전파정책 Class A~C | 트리거가 전부 **「신규 코퍼스 *곡* 인제스트」**, 경로도 `merge_batch_reanalysis`→`json_to_db` | ★**대상 아님.** Suno 출력이 아니라 외부 지시문 → **정책 공백** |
| 분류기준 §6 | 「attested 0이면 **DB 신설 금지** — 대장에만 기록」 | GAP 85는 정의상 attested 0 |
| 분류기준 §5 | 「표면지표는 confidence **med 상한**, 렌더 청취 전 attested 승격 금지」 | 전부 문자열 수집분 |

⇒ **지금 만들 수 있는 것은 정본이 아니라 「대장」이다.** §6이 그 자리를 이미 만들어 뒀다.

### 재발 방지 대상 (같은 사고 3회)
| 시점 | 사고 |
|---|---|
| 06-15→08-11 | GT 회신 표에 **채널 한정자 누락** → 2개월 뒤 encore가 커미션을 닫을 뻔 |
| 08-11→08-13 | 그 교훈을 **표 밖 박스**에만 적어 같은 자리에서 재발 → 「`[]` 쓰지 마십시오」 철회 |
| 08-13 | v1 **A등급 4건**을 못 읽은 출처·동일 저자 중복으로 계상 → 미검증 강등 |

★**세 번 다 「배포가 검증보다 빨랐다」.** 그래서 §3의 **한정자를 표 밖이 아니라 행 안에** 박는다.

---

## §1. 오늘 실측 4건 (전부 크레딧 0 · 신규 생성 0)

### TEST-1 — `(spoken)` 판정의 대조군 민감도
재현: `scripts/spoken_probe_sensitivity.py` → `data/exchange/spoken_probe_sensitivity.json`
새 측정 없음(기존 측정값 재조합). 판정규칙=사전등록 §5·§6 그대로.

| 클립 | 대조군 15조합 중 판정 분포 | 해석 |
|---|---|---|
| **RM2** | **말하기 14 / 보류 1** (93%) | ★**대조군 선택과 무관하게 견고** |
| **RM1** | 보류 8 / 말하기 5 / **노래 2** | ★**대조군 종속 — 판정 불가** |

- 단일 음성 극단: `Yuna`만 B극이면 **RM1이 「노래」로 뒤집힌다**. `Eddy`·`Grandma`면 「말하기」.
- ★**두 차례 실행이 뒤집혔던 원인 규명**: asrun1은 `Suhyun`이 `Yuna`로 폴백돼 **동일 오디오를 2표본으로 계상** → B극이 기울었다. 최종본이 중복 제거한 것이 맞다.
- ⇒ **방향은 안 뒤집힌다**(「노래」는 15중 2). **`(spoken)` 권고는 유지.** 다만 근거는 **「기계 2/2 검증」이 아니라 「기계 1/2 견고 + LEO 실청취(별개 재료) 4/4」**다.

### TEST-2 — GAP 85 자기검사
`scripts/metatag_narration_gap_v1.py` 재실행: **103개 중 OURS_BRACKET 3 / PARTIAL 11 / SP_ONLY 4 / GAP 85** 재현.
- ★**우려는 기각**: 우리쪽 집계는 대괄호 전용이 아니었다(`SP_SOURCES`에 `suno_sp_full` 포함, `norm()`이 괄호 제거). **v0의 정규식 결함이 우리 재고 집계엔 전이되지 않았다.**
- ★**대신 스테일 1건 발견**: 08-13에 미검증 강등한 `suno.com/s/nrhqq4oreDlBEabw` 4건이 스크립트에 **아직 `A_demo`로 남아 있다** → 헤드라인 「19건」은 **15건**이어야 한다.
- ★**내 의심이 틀렸음도 실측**: 나머지 A등급 출처 4개 유튜브 영상을 **오늘 처음 실물 회수**(`data/metatag_external/yt/verify_v1/`), 주장 태그 철자 **18/18 verbatim 적중**. 「안 보고 인용」이 아니었다.
  ⚠단 **적중한 것은 철자이지 「시연」이 아니다** — 영상 본문은 429로 미회수. A_demo 근거는 **철자 확인까지**.

### TEST-3 — 332 vs 323 재집계
재현: `scripts/metatag_lane_recount_v1.py` → `data/metatag_external/lane_recount_v1.json` (★08-13 후속 신설 — 아래 정정 참조)

- 원인 확정: **lane2 자기신고가 2 과대** · **lane4가 11 과소**. **어느 쪽도 정답이 아니다.** (자기신고 합 323 / 실물 항목 327)
- ★**아무도 안 센 수 = 고유 표기**: 정규화 후 **235~236종**. (항목 327 / (표기,출처) 쌍 306)
- ★**등급 분포 = `B_recited` 324 : `A_demo` 3** — **99.1%가 전언**. (고유 표기 단위로도 233:3)
- ★**중복 상위가 곧 파생 클러스터**: `spoken word` 15회·`whispers` 10·`female narrator` 8·`announcer` 7·`reporter` 6 — 전부 Bark 계보 복제. **중복 계상이 「교차확인처럼 보이는 착시」를 만든다.**
- ⇒ **인용 표준: 「고유 표기 235~236종(B_recited 99%)」 + 정규화 규칙 병기.** 「332」·「323」 단독 인용 금지.

> ★★**정정 (2026-08-13 후속, 자진)** — 이 절의 초판은 **「고유 표기 234종 / 고유 쌍 259」**였다.
>
> ⒜ **그 수치엔 재현 경로가 없었다.** 같은 §1의 TEST-1·TEST-2는 스크립트·JSON을 명시했는데 **TEST-3만 없었다.**
>   전파(§3 1단계 ⑵)하려고 재현기를 뒤늦게 만들었더니 **234도 259도 안 나온다**(엄격 236 / 관대 235 / 쌍 306).
>   ★어느 쪽이 틀렸는지는 **지금 확정 못 한다** — 그때의 절차 자체가 리포에 없기 때문이다.
>   ⇒ **재현되는 수치로 교체한다. 재현 없는 수치는 인용 표준이 될 수 없다.**
> ⒝ ★**단일 정수가 흔들림을 숨겼다.** 고유 표기 수는 **정규화 규칙에 따라 바뀐다.** 그래서 이제 **폭(235~236)과 규칙을 같이** 적는다.
> ⒞ ★**정규화 시안 하나가 다국어 레인을 지울 뻔했다** — 잡부호를 털려고 `[^a-z0-9가-힣 ]`를 쓰자
>   lane4의 `主持人`·`女性叙述者`·`耳语`가 **빈 문자열이 되어 한 종으로 합쳐졌다.**
>   다국어를 모으려고 만든 레인을 정규화가 지우면 그 레인은 없는 것과 같다. **문자 클래스 화이트리스트 금지**를 스크립트에 주석으로 박았다.

### TEST-4 — 소괄호 축 교차검산 (부분 성공 + 블로커)
- ★**우리 ASR엔 `[노래]` 마커가 전 8클립 0건** — 영상에서 갈렸던 그 축은 **우리 자료로 재현 불가**(검출기가 다름). 「반증」이 아니라 **「우리 자로는 못 잼」**.
- ★★**사전등록의 사실 오류 발견**: 「v2.3 3곡엔 `(spoken)` 0건」이라 적었으나 **뮤지컬 곡「그날도 오늘 같았죠」는 `(spoken)` 4건**을 쓴다(0건은 발라드·가스펠 2곡). ⇒ **M23a·M23b가 대상에서 잘못 빠졌고, 표본은 N=2가 아니라 N=4였어야 한다.**
- ~~★그러나 지금은 확장 불가 — 블로커~~ → ✅**08-13 후속 해소**: ASR 재실행(크레딧 0)으로 **곡 수준 4/4 확정**. ★**추측 매핑은 끝내 안 했다** — take 이름 a/b는 **미확정으로 남겼다**(원리상 못 가름). 확장에는 충분하다(집계는 a↔b 교환에 불변). 사전등록=`docs/vd_uuid_remapping_preregistration.md`
- ⇒ **자산 관리 결함 1건 등재**(§4 B-1).
- ★**취소한 주장 1건**: 중간에 「완벽한 미니멀 페어를 찾았다」고 판단했으나 **오판**이었다. `VD_LYRICS_v1`의 `(남, 대사)`는 설계문서 표기이고 **실제 입력(핸드오프)은 양쪽 다 `(spoken)`**이다. 대조군이 아니라 **같은 조건의 추가 표본**이다.

---

## §2. 등급 대장 — ★한정자를 행 안에 박는다

> 이 표는 **떼어서 인용될 것을 전제로** 만들었다. 각 행이 자기 한정자를 들고 다닌다.

| 표현·규칙 | 층 | 근거 | 등급 | ★행 안 한정자 |
|---|---|---|---|---|
| `(spoken)` 괄호 = 해당 행을 말로 | **입력층** | LEO 실청취 4/4(Dead Budget 04-25) + 기계 RM2 견고·RM1 불가 (재현=`scripts/spoken_delivery_probe_n4_report.py`) | **A** | 「★**판정 가능 표본 2클립 / 시도 4클립**(08-13 N=4 확장). 늘린 2클립은 **게이트 기각** — 뮤지컬 편곡은 가창부 sustain이 낮아 **자가 먼저 무너진다**. 통과분도 말하기 1·보류 1. **곡 수준 일반화 금지**」 |
| 듀엣 화자 R1~R6 | 입력·출력 혼합 | 코퍼스 3곡 실증 | **A** | 「곡 3건. 트로트·K-Indie·포크 — **장르 편중 미해소**」 |
| `narration`/`narrator`/`voiceover`/`monologue` → `spoken-word`로 | **출력층** | 전 채널 0/0 + help.suno.com 용어집 부재 | **A** | 「**출력층 계수**. 「입력에서 무효」가 아님」 |
| `spoken-word` SP 유효 | **출력층** | 23행/16형/**관측 9곡** | **A** | 「행수 23 ≠ 곡수. **곡 9**」 |
| `[]` 브라켓 맨 명찰 | — | 출력 1건 | **판정 보류** | ★「**입력층 미측정**. ~~쓰지 말 것~~ **08-13 철회**」 |
| Styles가 브라켓을 선행 서술 | 입력층 | 영상 8/8 | **B** | 「**단일 출처**. ★어느 필드가 먹였는지 **원리상 분리 불가**」 |
| 구분자 기능분화(`:`/`,`/`—`) | 입력층 | 영상 8/8 | **C(가설)** | 「단일 출처. **규칙 아님**」 |
| 긴 규칙문 브라켓 유효 | 입력층 | 영상 2예 vs hookgenius 반대 | **C(충돌)** | 「**양쪽 다 대조군 0**」 |
| 외부 나레이션 표기 **235~236종** | 입력층 | 4레인 수집(재현=`scripts/metatag_lane_recount_v1.py`) | **D(전언)** | 「**B_recited 99.1%**. 중복 상위=Bark 복제 → **교차확인 아님**. ★수는 **정규화 규칙 의존**이라 폭으로 적는다」 |
| `suno.com/s/nrhqq4…` 4건 | — | 못 읽음 | **미검증** | 「JS게이트·**실패양식 출처와 동일 저자**」 |

---

## §3. 정본화 범위 — 3단계

### 🟢 1단계 · 즉시 정본화 (오늘, 새 실측 불요)
1. **`metatag_narration_gap_v1` 스테일 시정** — A_demo 19→**15**, `suno.com` 4건을 `미검증` 등급으로 강등. 스크립트·JSON·문서 §2 동시.
2. **인용 표준 확정** — 「고유 표기 **235~236종**(B_recited 99%) + 정규화 규칙 병기」. `332`/`323` 단독 인용 금지. ✅**완료**(재현기 `scripts/metatag_lane_recount_v1.py` 신설 — ★234는 재현 실패로 폐기)
3. **§2 등급 대장을 `duet_bracket_grammar_v1.md`·`corpus_classification_criteria_v1.md`에 편입** — 단 **행 안 한정자 그대로**. ✅**완료**(분류기준 **§11 신설·v1.1** = 대장 본체 / 듀엣문법 **§8 = 포인터만**. ★복사 안 함 — 오늘 스테일 사고 2건이 「복사본은 한쪽만 늙는다」를 실증)
4. **`(spoken)` 근거 문구 정정** — 「기계 검증됨」이 아니라 「LEO 실청취 4/4 + **기계는 판정 가능 2클립 중 1클립만 견고**」. ✅**완료**(08-13 N=4 확장 후 문구 재갱신 — ★「N=2」라는 표기도 폐기했다. 늘린 2클립은 **게이트 기각**이라 분모가 「시도 4」가 아니라 **「판정 가능 2」**다)

### 🟡 2단계 · 조건부 (블로커 해소 후)
5. ~~**N=4 확장**~~ ✅**집행 완료**(08-13, 크레딧 0) — ★**그런데 「표본 2배」가 「근거 2배」가 아니었다.** 늘린 뮤지컬 2클립을 **사전등록 게이트가 기각**(분리도 0.1442·0.1311 < 0.15). ★**기각 사유 자체가 소득**: 뮤지컬 편곡은 가창부 sustain이 낮아 말 극과 노래 극이 붙는다 ⇒ **이 자는 편곡을 탄다.** ★문턱을 내려 통과시키지 않았다(값 보고 사전등록 고치면 무의미). 재현=`scripts/spoken_delivery_probe_n4_report.py`
6. **후보 선반 신설** — 235~236종을 `expr_*`가 **아닌** 별도 대장에. 필수 열: `grade`·`layer`·`source_author`·`derivative_cluster`(Bark 복제 플래그).
7. **못 읽은 37건 재개방** — `yt-dlp`가 유튜브를 열었듯 경로별 재시도.

### 🔴 3단계 · 생성 필요 (오너 승인 대기)
8. **2×2 파일럿**(스타일 서술 유무 × 브라켓 형태) — 설계·사전등록까지는 내 범위.
9. 그 결과로만 §2의 **B·C 등급 → A 승격** 가능.

---

## §4. 블로커·미결

| ID | 내용 | 해소 경로 | 소유 |
|---|---|---|---|
| **B-1** | `vd_duet3` uuid 4개 매핑 원장 부재 | ✅**부분 해소**(크레딧 0·ASR 재실행). ★**곡은 확정 4/4**(대조군 기지 4클립 복원 **4/4 통과**) — `2b33b2a6`=발라드 · `3776c8d9`=가스펠 · `66621da8`·`b6cb18a6`=**뮤지컬 v2.3 2 take**. ⚠**take 이름(M23a/b)만 미확정** — 같은 곡 2 take는 가사가 사실상 같아 **원리상 텍스트로 못 가른다**(마진 0.008). 해소=sunomusic 생성 원장 조회 | 재현=`scripts/vd_uuid_remap_v1.py`·`_posthoc.py` | 나 |
| **B-2** | 생성 크레딧 = 「설계까지만」(오너 08-12) vs 오늘 「테스트하고 진행」 지시 — **범위 확인 필요** | 오너 확인 | 오너 |
| **B-3** | ~~★엔진 간 정면 충돌~~ → ✅**편입 완료**(분류기준 **§4.1**). ★**「충돌」 판정을 내가 취소했다** — 둘이 같은 물음을 안 쟀다(Suno는 **Duration 락 180이 걸린** 하한 도달률 / Lyria는 락 기재 없는 초 평균·산포, 지표도 표본도 다름). 등재 명제는 「**분량→길이는 엔진 공통 법칙이 아니다**」 하나로 좁힘 | ✅완료. ★**08-14 후속**: B-1 해소로 **곡 내 take 산포**를 처음 쟀다 — **Suno도 7.8~16.8%**(19~36초) 흔들린다. ⇒ 「Lyria만 흔들린다」가 아니라 **둘 다·Lyria가 더**. ★**요건 창 30초 < take 폭 36초** ⇒ 단일 take 안착은 상당 부분 운. 잔여=**락 유무 통제 대조 0**(생성 필요 → **B-2 소관**) → C등급 유지 | 나 |
| **B-4** | 영상 자막 429 미회수 — A_demo의 「시연」 근거 미확정(철자만 확인) | 시차 두고 재시도 | 나 |

---

## §5. 배포 계획 — 누구에게 무엇을

★**현재 내 형식 문법이 실사용 중이다**(leomusic2 11:47 통지: encore도 「브라켓·화자 재지시·`(spoken)` 채널·금지 표기는 그대로 쓰라」로 유지). 그래서 배포는 **급하지 않되 정확해야** 한다.

| 순서 | 대상 | 내용 | 시점 | 회신 |
|---|---|---|---|---|
| ① | (내부) | 1단계 1~4 시정 커밋 | 오늘 | ✅**완료**(4항 전부) |
| ② | **encore** | ✅**발신 완료** 12:36 — 근거 강등(방향 불변)·N=4 확장의 **게이트 기각 2건**·★**편곡 의존성**(CM-1 나레이션 요건 직결)·내 집계문 자진 폐기·★**오전에 알고도 못 드린 한정 1건**(행수 조건부 이월·음절 미보장) | 1단계 직후 | 불요 |
| ③ | leomusic·2·3 | ✅**발신 완료** 12:37 — 3곳 **각각 직발신**(★cc는 실전달 아님). 인용 표준(235~236종·정규화 규칙 병기)+`(spoken)` 등급 강등+★「234종」 폐기 | ②와 동시 | 불요 |
| ④ | (보류) | 구분자 가설·긴 브라켓·235~236종 어휘 | ★**파일럿 전 배포 금지** | — |

★**②의 형식**: 08-11 사고 재발 방지로 **표를 보내지 않고 문장으로** 보낸다. 표를 보내야 하면 **한정자가 행 안에 있는 §2 표만** 보낸다. ✅**지켰다** — 4통 전부 표 0개, 문장으로만. 배달 검증=origin 본문 해시 4/4 일치(문구 대조 금지 규약).

---

## §6. 이 문서의 한계 (먼저 적는다)

1. 오늘 실측 4건은 **전부 기존 자산 재분석**이다. **새 관측은 0**이다.
2. `(spoken)` 결론은 **판정 가능 2클립**이다(08-13 확장 후에도 그대로 — 시도는 4, 게이트 기각 2). ★**「N=4로 늘었다」고 쓰면 거짓이다.** **곡 수준 일반화 금지.**
3. 235~236종은 **수집 표기 수**이지 **유효 어휘 수가 아니다.** 유효성은 하나도 안 쟀다. ★그 수조차 **정규화 규칙 의존**이다.
4. §2 등급 대장의 A등급은 **우리 자체 실측이 있는 것만**이고, 그 실측의 표본은 전부 **한 자릿수**다.
